<h3>Busqueda de Preguntas</h3>
<br>
<form action="quizes" method="get" >
  <input type="text" name="search" placeholder="Introduzca palabras">
  <input type="submit" value="Buscar" >	
</form>
<br>
<table>
  <% var i; for (i=0; i < quizes.length; i++) { %>
  <tr>
    <td><a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
    <td><a href="/quizes/<%= quizes[i].id %>/edit"><button type="button">Editar</button></a></td>
    <td>
      <form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
        <button type="submit" onclick="return confirm('Borrar: <%=quizes[i].pregunta%>');">Eliminar
        </button>
      </form>
    </td>
  </tr>
	<% } %>
</table>
<br>
<a href="/quizes/new"><button type="button">Crear Pregunta</button></a>